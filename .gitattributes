# GitHub classifies a whole file by its extension, so each .html file here is
# counted as HTML even though almost all of its bytes are JavaScript inside a
# <script> tag. Measured across the six files: 92% JavaScript, 8% HTML/CSS.
# This makes the language breakdown reflect what the code actually is.
*.html linguist-language=JavaScript
